ADD_EXECUTABLE(gentest
    gentest.c genres32.c gentest.h)
LINK_ICU_LIBRARIES(gentest ${TOOLUTIL_STUB})

IF(CMAKE_SYSTEM_NAME STREQUAL SunOS)
    SET_TARGET_PROPERTIES(gentest PROPERTIES LINKER_LANGUAGE CXX)
ENDIF()

