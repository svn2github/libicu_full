CONFIGURE_FILE("${CMAKE_CURRENT_SOURCE_DIR}/genccode.8.in"
               "${CMAKE_CURRENT_BINARY_DIR}/genccode.8")

ICU_SIMPLE_C_TOOL(genccode)

INSTALL(TARGETS genccode
        RUNTIME DESTINATION ${SBIN_DIR})
INSTALL(FILES "${CMAKE_CURRENT_BINARY_DIR}/genccode.8"
        DESTINATION ${MAN_DIR}/man8
        PERMISSIONS OWNER_READ OWNER_WRITE GROUP_READ WORLD_READ)

